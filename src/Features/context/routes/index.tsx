import type {RouteObject} from "react-router";
import ContextHome from "../page";

const ContextRoutes:Array<RouteObject> = [
    {
        path:'/context',
        element:<ContextHome/>
    }
]

export default ContextRoutes